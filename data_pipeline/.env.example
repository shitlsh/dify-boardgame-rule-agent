# data_pipeline environment variables — consumed by data_pipeline/scripts/*.py
# Copy this file to data_pipeline/.env and fill in real values.
# Never commit .env — it is already covered by .gitignore.
#
# For webapp variables (Next.js), see webapp/.env.example

# ── Google Gemini (data_pipeline/scripts/extract_rules_to_md.py) ─────────────
GEMINI_API_KEY=
